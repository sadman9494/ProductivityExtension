import ReactDOM from 'react-dom';
import React from 'react'
import Option from './option';

ReactDOM.createRoot(document.getElementById('optionPage-root')).render(
  <React.StrictMode>
    <Option/>
  </React.StrictMode>,
);
